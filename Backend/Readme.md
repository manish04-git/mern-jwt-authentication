express → web framework

mongoose → to connect with MongoDB and define models

bcryptjs → to hash passwords

jsonwebtoken → to create and verify JWT tokens

cors → to allow frontend (React, etc.) to call this API

dotenv → to load secrets (like DB URL, JWT secret) from .env file